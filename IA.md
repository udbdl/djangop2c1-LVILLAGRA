# Uso de Inteligencia Artificial

* **Herramienta:** Gemini[cite: 1]
* **Prompts utilizados:** "Genera la maquetación HTML de Bootstrap 5 para el detalle y la lista, y la lógica en views.py para vincular JSONs sin ORM basándote en la pauta oficial."[cite: 1]
* **Partes generadas utilizadas:** Código estructural HTML, funciones de carga de `json` en views.py y estructura de templates base y heredados[cite: 1].
* **Cambios propios aplicados:** Integración de la lógica en el proyecto local de Django (aplicación `dispositivos`), adaptación de rutas de carpetas (`data/`), pruebas de estrés variando archivos locales[cite: 1].
* **Verificación:** Ejecución de `python manage.py check` y pruebas manuales navegando entre el inicio y el detalle de zonas vacías y excedidas de límite[cite: 1].